name: NexDroid ROM Factory

on:
  repository_dispatch:
    types: [build_rom]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write

    steps:
      - name: ⬇️ Checkout Repo
        uses: actions/checkout@v4

      # ✅ THIS FIXES THE "OUT OF SPACE" CRASH
      - name: 🗑️ Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: 🛠️ Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip zip unzip aria2 jq erofs-utils
          # Make sure binaries are executable
          chmod +x bin/*

      - name: 🚀 Run Modder Engine
        id: build
        run: |
          # Redirect output to build.log for debugging
          bash mod.sh "${{ github.event.client_payload.rom_url }}" > build.log 2>&1
          
          # Check for success link
          LINK=$(grep "https://pixeldrain.com" build.log | tail -n 1)
          
          if [ -z "$LINK" ]; then
            echo "❌ No download link found. Build failed."
            exit 1
          fi

          echo "DOWNLOAD_LINK=$LINK" >> $GITHUB_ENV

      - name: ✅ Notify Success
        if: success()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.CHAT_ID }} \
          -d parse_mode=Markdown \
          -d text="✅ *Build Complete!* %0A%0A📦 [Download Here](${{ env.DOWNLOAD_LINK }}) %0A%0A_Flash this zip in TWRP or unzip for Fastboot._"

      # --- DEBUGGING SECTION ---
      
      - name: 🔍 Show Error Logs
        if: failure()
        run: |
          echo "::error::Build Failed! Dumping logs below:"
          cat build.log

      - name: ❌ Notify Failure
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.CHAT_ID }} \
          -d text="❌ Build Failed! I have printed the logs in the GitHub Actions tab. Go check them!"
